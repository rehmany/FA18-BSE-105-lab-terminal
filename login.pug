extends ../layout

block content
  h1 Login
  form(action="/login",method="post")        
        .form-group
            label(for="email") your Email
            input#email(type="text", class="form-control", name="email" value="fa18-bse-105@cuilahore.edu.pk")
        .form-group
            label(for="password") Your Password
            input#price(type="password", class="form-control", name="password", value="rehman")
        input(type="submit", class="btn btn-info" value="Login")
